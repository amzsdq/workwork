# B unit 08

A practical efficiency metric should separate worker-seconds from wall-clock throughput. parallelism_factor above 1 proves concurrent active worker time, but not necessarily valuable output. The stronger metric is accepted nonduplicate bounded units per wall-clock minute compared with a single-worker baseline using similar unit complexity. Both should be retained in later experiments.
