# R4 useful-work queue extension

R4 has completed the initial methodological audit set. To avoid the R3 queue-exhaustion defect, the next bounded work units remain directly tied to the program:

1. evaluate same-profile refinement design if 22m fails,
2. define what constitutes a credible external runtime ceiling versus duration failure,
3. audit planned-gap metric definitions against overlap behavior,
4. define minimum evidence for PRODUCTIVE_CAP distinct from OPERATING_CAP,
5. assess whether server commit timestamps can defensibly support lower-bound goal-directed spans without becoming the primary clock,
6. audit terminal close ordering against possible successor overlap,
7. prepare compact terminal evidence schema for R4,
8. re-read fresh canonical state before close to detect external interference.

These units are bounded and decision-relevant. Completion of this list still does not authorize voluntary close before the authoritative target condition.
