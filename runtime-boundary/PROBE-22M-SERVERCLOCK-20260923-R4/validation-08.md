# R4 target-progress guard

The repository's server-side `pushed_at` is later than START and continues to advance through substantive/validation work, but this value is not the WORKED clock. It is used only to confirm ongoing durable activity. END_MARKER remains the sole terminal duration endpoint.

No fresh anomaly or state conflict is present. No further redundant validation documents should be emitted until a material condition changes.
