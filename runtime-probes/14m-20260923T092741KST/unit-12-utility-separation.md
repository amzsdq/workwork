# Unit 12 — Utility/boundary separation

Probe: STRICT-14M-20260923T092741KST

The analytical utility model says a 14m target would need sufficiently high clean-close reliability to outperform the historical 10m baseline under its simplified assumptions, but that threshold is not empirical boundary evidence.

Decision discipline for this probe:
- classify 14m only from direct runtime/close evidence;
- do not use modeled utilization thresholds to declare PASS/FAIL;
- if 14m passes, advance coarse boundary search to 16m while leaving operating-cap promotion pending;
- defer actual utilization optimization until repeated close/wake data exist.

This prevents a favorable theoretical duty-cycle result from biasing Phase-A classification.
